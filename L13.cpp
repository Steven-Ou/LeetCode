#include <iostream>
#include <vector>
using namespace std;

class Solution{
    public: 
        int romanToInt(string s){
            int I = 1;
            int V = 5;
            int X = 10;
            int L = 50; 
            int C =100;
            int D = 500;
            int M =1000;
            vector <int> store; 
            for(int i = 0; i< s.length(); i++){
                char ch = s[i];
                
                if(ch == 'I'){
                    store.push_back(I); 
                }
                else if{
                    
                }
            }
        }
}